.DS_Store
node_modules/

# backend credentials and private booking data — never commit
.sheet.local.json
*.local.json
sheet-backup-*.json
*.local.gs

# trip content — the itinerary lives in the Google Sheet, not in this public
# repo. src/seed.fixture.json is the invented stand-in the tests use and IS
# committed; these are the real thing.
src/seed.json
src/seed-*.json
src/schedule.json
